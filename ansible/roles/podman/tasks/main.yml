---
- name: Install Podman and related tools
  dnf:
    name:
      - podman
      - skopeo
      - buildah
      - slirp4netns
      - fuse-overlayfs
      - policycoreutils-python-utils
    state: present

- name: Get deploy user info
  user:
    name: "{{ deploy_user }}"
  register: deploy_user_info

- name: Enable linger for deploy user
  command: "loginctl enable-linger {{ deploy_user }}"
  args:
    creates: "/var/lib/systemd/linger/{{ deploy_user }}"

- name: Create Quadlet directory for deploy user
  file:
    path: "/home/{{ deploy_user }}/.config/containers/systemd"
    state: directory
    owner: "{{ deploy_user }}"
    group: "{{ deploy_user }}"
    mode: '0755'

- name: Create systemd user directory
  file:
    path: "/home/{{ deploy_user }}/.config/systemd/user"
    state: directory
    owner: "{{ deploy_user }}"
    group: "{{ deploy_user }}"
    mode: '0755'

- name: Set permanent SELinux fcontext for quadlet directory
  community.general.sefcontext:
    target: '/home/{{ deploy_user }}/.config/containers/systemd(/.*)?'
    setype: container_file_t
    state: present
  when: ansible_selinux.status == 'enabled'

- name: Apply SELinux fcontext to quadlet directory
  command: restorecon -Rv /home/{{ deploy_user }}/.config/containers/systemd/
  when: ansible_selinux.status == 'enabled'
  changed_when: false

- name: Ensure podman socket is started and enabled for deploy user
  systemd:
    name: podman.socket
    scope: user
    state: started
    enabled: yes
  become: yes
  become_user: "{{ deploy_user }}"
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ deploy_user_info.uid }}"

- name: Create podman networks
  containers.podman.podman_network:
    name: "{{ item }}"
    state: present
  loop:
    - proxy-net
    - monitoring-net
  become: yes
  become_user: "{{ deploy_user }}"
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ deploy_user_info.uid }}"
